package Controle;

import Componente.Cadeira;

public class ControleMesa {
/*
    private int quantLugares; //(quantidade de lugares)


    //referencias de definição e abstração dos componentes (aos espaços):
    //  ->indices devem indicar o ID local dos componentes;
    //  ->valores devem indicar o estado de acesso que está sincronizado
    //      (0 para vago, 1 para ocupado);
    private int[] refCadeira;
    private int[] refGarfo;
    private int[] refPrato; //  ¿necessário?

    public ControleMesa(){

    }

    public int buscarLugar() throws Exception{
        for(int i = 0; i < this.quantLugares; i++){
            if(this.refCadeira[i] == 0)
                return i;
        }

        throw new Exception(".exception - Nenhum Lugar disponível.");
    }

    public int buscarLugar(int id) throws Exception{
        if (this.refCadeira[id] == 0)
            return  id;

        throw new Exception(".exception - Lugar indisponível.");
    }

    public Cadeira ocuparCadeira() throws Exception{
        int idLugar;
        idLugar = this.buscarLugar(); //check pass...

        //cria cadeira e determina ocupação do lugar
        this.refCadeira[idLugar] = 1;
        this.lugares.add(new Cadeira(idLugar));

        return null;
    }

    public Cadeira ocuparCadeira(int id) throws Exception{
        int idLugar;
        idLugar = this.buscarLugar(id); //check pass...

        //cria cadeira e determina ocupação do lugar
        this.refCadeira[idLugar] = 1;
        this.lugares.add(new Cadeira(idLugar));

        return null;
    }

 */
}
